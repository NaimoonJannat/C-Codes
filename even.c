#include <stdio.h>

int main() {
   int n;
   scanf("%d",&n);
   fflush(stdin);
   char s[1000];
     //for(i=0;i<n-1;i++)
     fgets(s, 1000, stdin);
     for(i=0;i<n-1)
     puts(s);
    return 0;
}

